# V0.6.7 Recovery Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development task-by-task. The user has explicitly authorized the supplied handoff and this reconstruction scope; no further scope approval is required.

**Goal:** Restore editable Text/Manga/Materials on the sole v0.6.9 recovery baseline.
**Architecture:** Extend the existing discriminated layer tree, compositor, history, and version-1 project codec. New content remains editable metadata; material pixels are embedded losslessly and no external URL is needed to reopen. Preserve existing raster/vector/assist behavior and explicit recovery.
**Tech Stack:** TypeScript 5.6.3, Canvas 2D, native DOM, Node tests, Playwright Chromium.
**Spec:** docs/recovery/DRAWING_STUDIO_WORK_CODEX_HANDOFF_2026-09-17.md sections 6.1, 13, 15, 16, 19; version-specific reference files beside it.

## Global Constraints
- Only source baseline: drawing-webapp-v0.6.9-base.zip, SHA256 be7afc35e91d2014a5c218db72e678c7af5633a2bfc8627fb67d38eac7f5d52d.
- Restore V0.6.7 only. Do not implement V0.6.8 or later scope.
- Preserve V0.6.9 Open / Save / Save As, formatVersion 1, IndexedDB autosave/recovery, dirty tracking, unknown extensions.
- Every displayed new control must perform its advertised operation. Existing 184 tests must continue passing.
- Production code uses native DOM and Canvas; do not migrate framework or add runtime dependencies.
- Freehand balloons, arbitrary polygon panels, marketplace, font-file embedding are outside this checkpoint.

### Task 1: Editable layer engine and persistence
**Files:** create src/drawing/editableLayers.ts and src/drawing/editableLayerRenderer.ts; modify src/drawing/layers.ts, src/components/DrawingCanvas.ts, src/persistence/documentDto.ts, src/persistence/projectFormat.ts; minimal type narrowing in layerPanelMarkup.ts if necessary. Add tests/editableLayersV067.test.mjs and tests/editablePersistenceV067.test.mjs. No App/UI redesign in this task.
**Interfaces:** export EditableLayerKind = 'text'|'balloon'|'panel'|'screen-tone'|'manga-effect'|'material'; EditableLayerData discriminated union; expose addEditableLayer(state, data) and updateActiveEditableLayer(state, patch) in layers.ts. DrawingCanvas exposes addEditableLayer(data), updateEditableLayer(patch). Provide defaults helper createEditableLayerData(kind,width,height) except material requires imported asset. Report exact signatures for task 2. Use typed node metadata with validated finite bounds and immutable/deep cloning. Renderer may accept canvas factory.
- [ ] Write model tests first; run to observe missing-feature failure. Example behaviors:
```js
assert.equal(findLayerNode(addEditableLayer(createInitialLayerDocument(), textData), 'text-1').kind, 'text');
assert.equal(original.nodes.at(-1).content, 'Original'); // duplicate edits do not alter original
assert.throws(() => validateEditableLayerData({...toneData, frequency:0}));
```
- [ ] Implement six kinds with full required properties: text content/font family/size/weight/alignment/line height/letter spacing/fill/outline color+width/x/y; balloon ellipse/rounded rectangle body geometry and straight tail endpoint/base size/fill/stroke; panel rows/columns/margin/gutter/stroke; screen tone dots/lines frequency/angle/density/color; deterministic seeded speed/focus effect count/width/length/center/angle; material image/pattern/texture position/scale/rotation/repeat and embedded validated RGBA asset.
- [ ] Integrate real rendering into renderSiblingSet before raster fallback. Transparent generated canvas participates in group opacity, visibility, blending, clipping base, Navigator, export, flatten. Text render Unicode/newlines; stroke outline; consistent spacing/alignment. Tone/effect work is bounded and deterministic. Material renderer caches decoded assets, evicts unused content, and uses transform on patterns and images.
- [ ] Replace raster performText with editable creation while keeping text defaults. Snapshot, duplicate, export/import deep clone metadata and assets. Existing file format stays version 1 and unknown extensions persist. Validate new metadata and material dimensions/byte length before mutating active document. Avoid premature canvas resize on invalid import; reject corrupt new content safely.
- [ ] Test round-trip of all six kinds plus material pixel bytes, nested clone/duplicate isolation, history via actual canvas if browser available, deterministic geometry, and invalid data. Keep tests behavioral, no new source-grep tests. Run focused tests, then full npm test and npm run build. Commit only owned files and write report.

### Task 2: Editable tools and compact workspace UI
**Files:** create src/components/MangaPanel.ts and src/components/EditableLayerProperties.ts or similarly focused components; modify App.ts, ToolBar.ts, PropertiesPanel.ts, LayerPanel.ts/layerPanelMarkup.ts, editor/types.ts, persistence/editorStateDto.ts, styles.css. Modify DrawingCanvas only for placement wiring if needed. Tests: browser-driven UI tests or DOM integration tests exercising real callbacks.
**Interfaces:** consume Task 1 exported layer APIs and report. New layer kinds and their fields follow Task 1; do not invent alternate metadata.
- [ ] Write a failing UI behavioral test that creates text, changes content after placement, and checks exported metadata and changed pixels.
- [ ] Add Text/Balloon/Panel/Tone/Effect/Material rail entries. Manga/Materials primary palette provides actual add actions (including Speed, Focus and image/pattern/texture import). Image import accepts PNG/JPEG/WebP and embeds pixels; malformed import produces visible error and no partial layer. Material rail opens import; other rail tools place editable content on canvas. Existing text defaults remain editable prior to placement.
- [ ] Wire post-placement property controls for every field. Selected layer kind determines editable properties independent of active tool. Preserve focus/caret during text editing and support multiline/IME without rerendering inputs each keystroke. Use change/commit semantics for history. Include layer badges TXT/BAL/PNL/TONE/FX/MAT and kind filters. Preserve all existing real commands.
- [ ] Match reference information hierarchy: connected charcoal docks, compact controls, Manga/Materials/Text/Material primary, Properties/Layers secondary. Keep drawing color controls available, and keep Navigator/Save/Open visible. Avoid overflowing offscreen tools/docks; support desktop and tablet widths plus handedness mirroring.
- [ ] Run focused browser interaction test, full npm test and npm run build. Commit and report exact selectors/workflows for final verification.

### Task 3: Browser acceptance and checkpoint
**Files:** tests/browser-v067.mjs or scripts/verify-v067.mjs, docs/recovery/V067_CHECKPOINT.md, package.json/lock for pinned reproducible tooling, final archive/evidence outside repo.
- [ ] Use real Chromium canvas and IndexedDB. Create all six layer kinds through UI; edit, duplicate, delete, reorder, undo/redo; verify rendered pixels, opacity/blend/visibility, Navigator, flatten and draft-excluding PNG export. Confirm assists do not export.
- [ ] Save/Open .drawstudio with nontrivial material pixels; compare data and exported PNG pixels. Reload and explicitly recover IndexedDB checkpoint, then compare metadata/pixels. Corrupt material load must leave current document intact.
- [ ] Capture actual UI and historical static HTML at same viewport; visually inspect and document preserved hierarchy/intentional differences. No fake completion.
- [ ] Run full npm test, npm run build; commit. Create nonempty source ZIP and git bundle, include tests/docs, exclude node_modules/temp. Verify CRC and entries; extract into fresh directory, npm ci, npm test, npm run build from delivered copy.
- [ ] Record source baseline hash, restoration scope, commits, test counts, browser evidence, build results, known limits, archive SHA256/size. Persist final ZIP and checkpoint/evidence and return links. Stop at V0.6.7.
